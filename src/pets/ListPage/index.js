import React from 'react';
import PetList from "../../container/pet-list";

const PetListPage = () => {
    return (
        <div>
            <PetList/>
        </div>
    );
};

export default PetListPage;